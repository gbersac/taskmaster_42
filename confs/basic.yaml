# blop:
#     cmd: "tail -f ls.txt"
#     numprocs: 3
#     autorestart: never
#     stdout: "tail.txt"

#should print the ls of tmp
# test_workingdir:
#     cmd: "ls"
#     workingdir: /tmp
#     env:
#         STARTED_BY: taskmaster
#         ANSWER: 42

# the permission of test_umask should be none
# test_umask:
#     cmd: "rm -f test_umask; touch test_umask ; ls -l test_umask"
#     umask: 777

# quitting the program must display sig_quit
test_stopsignal:
    cmd: "trap 'echo sig_int' 2 ; trap 'echo sig_quit' 3 ; tail -f ls.txt"
    stopsignal: quit

# it must attempt 3 time to relaunch this program (total tries = 4)
# test_startretries:
#     cmd: "cat no_file"
#     startretries: 3

