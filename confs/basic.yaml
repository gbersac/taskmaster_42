# blop:
#     cmd: "tail -f ls.txt"
#     numprocs: 3
#     autorestart: never
#     stdout: "tail.txt"

glup:
    cmd: "ls"
    workingdir: /tmp
    env:
        STARTED_BY: taskmaster
        ANSWER: 42
    # autostart: False
