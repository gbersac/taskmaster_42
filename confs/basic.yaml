# blop:
#     cmd: "tail -f ls.txt"
#     numprocs: 3
#     autorestart: never
#     stdout: "tail.txt"

glup:
    cmd: "echo $PATH"
    env:
        STARTED_BY: taskmaster
        ANSWER: 42
    # autostart: False
